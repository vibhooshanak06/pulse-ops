# Data processing
pandas==2.2.2
numpy==1.26.4

# Machine learning
scikit-learn==1.4.2

# Database access (reads metric_aggregates, writes anomalies)
sqlalchemy==2.0.30
psycopg2-binary==2.9.9

# Settings / env
pydantic-settings==2.2.1
python-dotenv==1.0.1

# Scheduling (anomaly detection runs on a schedule)
apscheduler==3.10.4

# HTTP client (calls backend API if needed)
httpx==0.27.0
